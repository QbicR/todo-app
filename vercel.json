{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "builds": [
    {
      "src": "json-server/index.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["json-server/db.json"]
      }
    }
  ]
}
